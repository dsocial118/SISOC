[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
addopts = --reuse-db
markers =
    smoke: quick smoke/integration checks
    mysql_compat: tests de compatibilidad ejecutados sobre MySQL real en CI
