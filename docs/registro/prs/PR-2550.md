# PR #2550 - Celiaquia tk 2152

## Metadata

- PR: https://github.com/dsocial118/SISOC/pull/2550
- Base: `development`
- Rama origen: `CeliaquiaTk_2152`
- Autor: `MariaNavarro90`

## Resumen operativo

- Tipo de cambio: feature
- Área principal: celiaquia — validación RENAPER (vista, template y JS; sin modelos ni migraciones)
- Contexto funcional: Celiaquía — validación RENAPER de un legajo. Cuando el domicilio de RENAPER no coincide con el presentado por el beneficiario, el técnico necesita saber a qué versión del DNI corresponde el dato del servicio para decidir cuál es el vigente.
- Resumen para changelog: La validación RENAPER ahora muestra la fecha de emisión, el ejemplar y el vencimiento del DNI, como referencia para resolver discrepancias de domicilio.
- Impacto usuario: Sólo lectura y sólo para técnicos y coordinadores que usan la validación RENAPER. No cambia datos, estados ni flujos: agrega información al modal existente. Por la medición de producción, la franja va a verse en prácticamente todas las validaciones.

## Áreas afectadas

- `celiaquia`
- `docs/registro`
- `static`

## Archivos relevantes del diff

- `celiaquia/templates/celiaquia/expediente_detail.html`
- `celiaquia/tests/test_validacion_renaper_ejemplar.py`
- `celiaquia/views/validacion_renaper.py`
- `docs/registro/cambios/2026-09-21-2152-fecha-emision-dni-renaper.md`
- `static/custom/js/expediente_detail.js`

## Validación declarada

- Pruebas automáticas: docker compose exec django pytest celiaquia/tests/test_validacion_renaper_ejemplar.py -v → 15 passed. docker compose exec django pytest celiaquia/ → 301 passed, sin regresiones. Los tests son de función pura (sin DB ni red) y cubren: payload con el formato real dd/mm/aaaa, fecha ISO, payload parcial, placeholders, formato de fecha inesperado y datos_api ausente o de tipo inválido. black y pylint en verde. djlint sobre expediente_detail.html reporta el archivo completo como reformateable, pero ya lo hacía antes de este cambio: se dejó como estaba para no mezclar un reformateo de 1755 líneas con la feature.
- Pruebas manuales: 1. Entrar como usuario coordinador o técnico (con un superuser el botón no aparece: la condición del template es is_coord or is_tec). 2. Abrir un expediente en estado EN_ESPERA, ASIGNADO o PROCESO_DE_CRUCE con legajos en PENDIENTE o SUBSANADO — son las condiciones bajo las que se muestra el botón. 3. Expandir el legajo (botón del ojo) y hacer clic en "Validar Renaper". 4. Verificar que dentro de la tarjeta verde "Datos de Renaper", arriba de la tabla, aparezca la franja: *"DNI emitido el dd/mm/aaaa · ejemplar B · vence el dd/mm/aaaa. Los datos de arriba corresponden a este ejemplar."* 5. Verificar que la comparación de domicilio siga funcionando igual (filas en amarillo cuando provincia y RENAPER difieren). 6. Caso negativo: con un legajo cuya respuesta no traiga los campos, la franja no debe aparecer y el resto del modal debe funcionar normal.

## Riesgos y rollback

- Bajo. Sin migraciones ni cambios de modelo, el rollback es revertir el commit. No se tocó el mapeo compartido de core, así que otras apps no se ven afectadas. El campo se lee de forma defensiva: si el servicio dejara de mandarlo o cambiara el formato, la UI oculta el bloque o muestra el valor crudo, sin romper la validación.

## Documentación sugerida para lectura

- `docs/indice.md`
- `docs/ia/CONTEXT_HYGIENE.md`
- `docs/ia/ARCHITECTURE.md`
- `docs/ia/TESTING.md`

## Notas para continuidad

- Este documento es generado automáticamente desde el contexto del PR y sirve como índice rápido para revisión y futuras sesiones de agentes.
- Si la metadata del body del PR está incompleta, varias secciones usarán fallbacks derivados del título o del diff.
