## Inicio: logo INET para grupos VAT

- Se restauró el logo institucional anterior del header compartido (`logoBlanco.png`).
- En la pantalla de `inicio` se muestra `LOGOS-INET-SE_BLANCO.png` dentro de un bloque centrado con fondo oscuro para usuarios autenticados que pertenezcan a `CFP`, `CFPJuridicccion` o `CFPINET`.
- El resto de los usuarios mantiene la presentación visual anterior del inicio.
- Se reutiliza un filtro de template para evaluar pertenencia a múltiples grupos desde las vistas compartidas.