# VAT - selector ver en cursos y comisiones del centro

- Se agrego el selector `Ver` en la tabla `Cursos configurados para este centro` del panel de cursos del centro.
- Se agrego el selector `Ver` en la tabla `Comisiones de Curso` del mismo panel.
- Ambos selectores limitan la cantidad de filas visibles sobre el resultado ya filtrado del lado cliente.