# Pending release notes

Los archivos de esta carpeta se generan automáticamente para PRs con base `main`.

Convención:

- `YYYY-MM-DD-pr-<numero>.md`

Uso:

- consolidar notas preliminares del deploy a producción
- regenerar `CHANGELOG.md` para la release objetivo
