from .models import Programas


def insertar_programas():
    None
    # Borrar todos los registros existentes en la tabla
    # Programas.objects.all().delete()  # O puedes usar Programas.objects.all().truncate() si deseas vaciar la tabla completamente

    # Crear nuevos registros
    # Programas.objects.create(id=23, nombre='Centros de desarrollo Infanto Familiar',fk_subsecretaria_id=35, observaciones='')
    # Programas.objects.create(id=24, nombre='Mil Días',fk_subsecretaria_id=35, observaciones='')
    # Programas.objects.create(id=25, nombre='Camino de la Embarazada',fk_subsecretaria_id=35, observaciones='')
    # Programas.objects.create(id=26, nombre='Proyecto de Vida',fk_subsecretaria_id=35, observaciones='')
    # Programas.objects.create(id=21, nombre='Servicio Local',fk_subsecretaria_id=32, observaciones='')
    # ?????Programas.objects.create(id=, nombre='Medidas de abrigo',fk_subsecretaria_id=32, observaciones='')
