services:
  django:
    build:
      context: .
      dockerfile: ./docker/django/Dockerfile
    environment:
      TZ: America/Argentina/Buenos_Aires
    volumes:
      - .:/sisoc/
    restart: unless-stopped
    ports:
      - "8001:8000"
    env_file:
      - .env
