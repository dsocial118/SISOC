# Generated by Django 4.2.20


from django.db import migrations, models


def migrate_nacionalidad_from_ciudadanos(apps, schema_editor):
    """Copiar Nacionalidad desde ciudadanos a core preservando los IDs."""
    try:
        NacionalidadOld = apps.get_model("ciudadanos", "Nacionalidad")
    except LookupError:
        return

    NacionalidadNew = apps.get_model("core", "Nacionalidad")
    db_alias = schema_editor.connection.alias

    for old in NacionalidadOld.objects.using(db_alias).all():
        NacionalidadNew.objects.using(db_alias).update_or_create(
            id=old.id, defaults={"nacionalidad": old.nacionalidad}
        )


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0003_programa_and_migrate_data"),
    ]

    operations = [
        migrations.CreateModel(
            name="Nacionalidad",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                ("nacionalidad", models.CharField(max_length=50)),
            ],
            options={
                "verbose_name": "Nacionalidad",
                "verbose_name_plural": "Nacionalidades",
            },
        ),
        migrations.RunPython(
            migrate_nacionalidad_from_ciudadanos, migrations.RunPython.noop
        ),
    ]
